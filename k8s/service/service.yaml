apiVersion: v1
kind: Service
metadata:
  name: security-monitor
  namespace: security-monitor
  labels:
    app: security-monitor
spec:
  selector:
    app: security-monitor
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
